services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=sqlite:///./todos.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload